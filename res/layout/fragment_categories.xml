<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".App" >

    <TextView
        android:id="@+id/categoriesTitle"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="@string/log_ingredients_title"
        android:textSize="@dimen/title_font_size" />

    <include 
        layout="@layout/component_alphabet_buttons"
        android:id="@+id/alphabetButtons"
        android:layout_above="@+id/categoryLV" />
     
    <ListView
        android:id="@+id/categoryLV"
        android:layout_width="fill_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="@dimen/list_margin_top_size" >

    </ListView>

</RelativeLayout>